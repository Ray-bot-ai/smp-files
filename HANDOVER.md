# 交接指南 · SMP 档案重做 OCR

写给**接手这个项目的人或 AI 智能体**。读完这份应该能独立接着干，不需要回看聊天记录。

> 项目背景、模型选型、成本、三条铁律见 [README.md](README.md)。本文只讲**怎么干、干到哪了、下一步干什么**。

---

## 0. 一句话现状（2026-08-19 13:00 实测）

3,866 件、**92,769 页**上海工部局警务处档案，用 `qwen3.7-plus` 逐页重做 OCR
并生成中文对照，已发成可检索的 GitHub Pages 站点。

**转录 82,131 页（88.5%）· 译文 77,061 页 · 剩 858 件 / 11,592 页 · 已花 ¥788.60。**

跑批**正在跑**（`run_chunked.py --chunk 40`，第 3/24 批）。
**但它跑完当前批就会自动停机**——原因见下面第 5 节。

查现状永远用这一条，别信文档里的数字：

```bash
.venv/bin/python monitor.py
```

---

## 1. 环境与凭据

```bash
cd ~/projects/smp-vlm-ocr     # .venv 已建好，直接用 .venv/bin/python
```

**凭据都不在仓库里：**

| 用途 | 从哪来 | 怎么用 |
|---|---|---|
| 百炼 API（主端点） | `DASHSCOPE_API_KEY` 环境变量；没有则从 `LLM_OCR_VAULT` 指向的 Obsidian 库读 llm-ocr 插件配置 | `common.py` 自动处理 |
| 备用端点 | `FALLBACK_API_KEY` / `FALLBACK_BASE_URL` / `FALLBACK_MODEL`，否则读插件的 `Custom2` | 同上 |

仓库根目录的 `.env`（已 gitignore）会被 `common.py` 自动读取，**不用每次 export**。

**绝对不要**把 key 写进仓库、聊天或提交信息。GitHub 推送用 `gh auth login` 的钥匙串凭据。

---

## 2. 日常操作（就这几条）

```bash
# 启动跑批 —— 一定用这个，别直接 python run_chunked.py
bash scripts/relaunch.sh
```
> 它做两件必须做的事：`unset PYTHONPATH PYTHONHOME`（Hermes 终端自带的
> PYTHONPATH 指向 python3.11 venv，会让项目的 3.13 进程 import numpy 时
> ABI 不匹配**启动即崩**）；`nohup + caffeinate` 摘掉 SIGHUP 并防睡眠。
> 日志写到 `/tmp/smp-run.log`。

```bash
.venv/bin/python monitor.py            # 只读，看进度与异常信号
.venv/bin/python monitor.py --watch    # 每 5 分钟刷一次

.venv/bin/python fill_zh.py            # 只补译文缺口（转录已完成的件）
.venv/bin/python run_chunked.py --repair   # 全库补跑「还能救」的失败页
.venv/bin/python run_chunked.py --stat     # 只查剩多少（不写数据，可随时跑）

.venv/bin/python build_site.py         # 重建站点索引（数据更新后跑这个）
.venv/bin/python make_review.py        # 刷新人工复核队列
bash scripts/stage_safe.sh             # 安全暂存后再 commit
```

**提交数据文件必须走 `scripts/stage_safe.sh`**，别 `git add data/`——
跑批正在写的文件会被提交成半截状态。它只暂存「15 分钟前完成且校验通过」的。

**发布站点** = 推到 `origin/master`，GitHub Pages 从 `docs/` 直接发。
推送前先问用户。

---

## 3. 已经验证过的事实（别再重测，浪费钱和时间）

| 结论 | 证据 |
|---|---|
| **必须用原生分辨率 `_w2400`** | 降到 1000px 会让模型从「认错」变成「编造」，见 README 对照表 |
| `qwen3.7-plus` 是最优选择 | 6 页盲测 + 关键事实命中 |
| `qwen3.5-ocr` **不能用** | 专用 OCR 模型反而全场最差，0/6 |
| **全库确数 3,866 件 / 92,769 页** | 清单跑完（不是外推），取图 31.7 GB |
| 页数别按 3,941 算 | 其中 60 件 `smpa-N` 是缩微整卷，与散件内容重复。唯一件是 3,878 |
| 空转录是偶发的，重试就能拿到 | 2061/n25、674/n6 主端点第一次返回 0 字，重试拿到 328/1,646 字 |
| 内容审查是**确定性**的，重试无用 | 3311/n4 连拒两次，换端点后一次成功 |
| 触发审查的是**色情不是政治** | 3311/n4 是风化案卷；4 件最敏感的政治监视卷宗 4/4 通过 |
| **备用端点关不掉思考** | 逐件统计：有 reasoning 的件**全部**走过备用端点，没走过的 0 件有。八种写法均无效。**非故障** |
| 主端点 `enable_thinking:false` 一直有效 | 同上统计的另一半 |
| **简繁通搜方案可行** | 归一化 bigram；简体「国民党」与繁体「國民黨」命中完全一致 |
| 模型会自相矛盾，且这是检测信号 | 同件内同一人名三种写法，必有两种错 |
| **但档案原件本身也不一致** | 同件里打字员写 Waung Tsung Hiang、标题写 Wang Cheng-Hsiang、手写「汪正祥」。自动检测**分不清「模型转错」与「原件本就如此」**，只能定位、不能判定 |
| **模型会退化成重复循环** | 翻译一页中共传单吐出 1,349 次重复「□」，撑爆 max_tokens 导致后一页译文整页消失，**全程不报错** |
| 旋转页几何检测走不通 | 三种判据全失败，见第 6 节 |

---

## 4. 文件地图

```
common.py         配置 + API 封装 + 取图 + 转录提示词（改提示词改这里）
demo_run.py       ★ 核心：run_ocr / run_translate / degenerate 退化检测 / _missing_zh 缺口
run_chunked.py    ★ 分批跑批主入口，含 --repair
fill_zh.py        只补译文缺口
monitor.py        只读监控
build_site.py     建站数据生成（检索索引在这里，见第 6 节）
make_review.py    生成人工复核队列
imgqual.py        影像成像指标（必须在删图前算）
glyphnorm.py      简繁异体字形归一（检索用）
run_all.sh        fill_zh + run_chunked 串行
scripts/relaunch.sh   ★ 跑批启动器，用这个启动
scripts/stage_safe.sh ★ 安全暂存
manifest.jsonl    全量清单，一行一件
data/*.json       ★ 核心产物，一件一个：每页 en/zh/error/tries/image_url
unusable_pages.json 人工判定放弃的页（流水线与站点两头都认这一份）
imgqual.json      影像指标（gitignore，6.8MB）
docs/             GitHub Pages 站点
images/           页面影像（gitignore，跑完即删）
```

**外部依赖**：`~/projects/smp-archive/catalog.json`（3,941 件目录，别删）。

---

## 5. 当前进度与下一步

### 🔴 立刻要处理的：跑批会自动停机

08-19 09:28–10:20 机器断网（DNS 解析失败）。后果：

- 下一批的预取图 **0/457 页**
- 4 件共 347 页转录全失败（`in=0 out=0`，**请求没发出去，没烧钱**）
- 391 次翻译分块 `URLError`，21 件「翻译全失败」

`run_chunked.py` 有条硬规矩：整批取图成功率 <50% 判定为 archive.org 限流并**停机**。
所以第 3 批跑完，进程就会打印停机提示退出。**这是保险不是故障。**

**怎么办**：确认网络正常后重跑 `bash scripts/relaunch.sh`。已完成的自动跳过；
那 347 页 `tries=1`（上限 3）且错误属「可修复」，影像已保留，会自动重取重转。
**别手动去补**。

### ⬜ 收尾清单（按顺序）

1. **跑完剩下的 858 件 / 11,592 页** —— 重启跑批即可，约 ¥115。
2. **补译文缺口** —— `fill_zh.py`。当前缺 4,291 页 / 33 件，其中大部分是
   「转录跑在翻译前面」的正常现象，但那 21 件「翻译全失败」是断网造成的，必须补。
3. **全库 `--repair`** —— 把所有「还能救」的失败页扫一遍补上。
4. **重建站点 + 发布** —— `build_site.py` → `make_review.py` →
   `stage_safe.sh` → commit → **问过用户再 push**。
   站点数据现停在 08-18 18:59（2,935 件），落后本地约 80 件。
5. **抽样校验（最高优先，仍未做）** —— 随机抽 30–50 页人工比对原件影像，
   按字段分类统计（数字/日期/人名/地名/普通词），**区分三类**：
   ①模型转错 ②原件本身不一致 ③原件残缺无法判定。
   任务单在 https://ray-bot-ai.github.io/smp-files/verify.html
   做完把 `quality.html` 上基于个位数样本的说法换成有分母的数字。

### 📌 未推送

本地领先 `origin/master` 若干个提交（含人工复核工作台 `workbench.html`）。
`git log --oneline origin/master..HEAD` 看。推送 = 站点更新，**先问用户**。

---

## 6. 站点与检索设计

https://ray-bot-ai.github.io/smp-files/ · 页面：
`index`(关于) `search`(检索) `browse`(全库目录) `quality`(质量报告)
`file.html?id=<短号>`(逐页三栏阅读) `workbench.html`(人工复核工作台)
`verify.html`(抽样校验任务单) `triage.html`(人工扫图队列)

**检索是自己实现的，不要换成 Pagefind**：它对 CJK 只做字符切分、没有字形归一化，
搜「国民党」命不中原件里的「國民黨」——这批档案全是繁体，这条是致命的。
方案：中文走**归一化 bigram**（字形先经等价表归一，再取二字滑窗，不需分词器），
英文按词。字形表复用用户自建 Obsidian 插件「简繁异体通搜」的 `variants.json`
（6,963 字头，cjkvi-tables），另补 16 组**新旧字形**（吴/吳、吕/呂 等）。

**两端分词与分片必须严格一致**：`build_site.py` 的 `tokens()`/`shard_of()` 与
`docs/assets/search.js` 的 `tokenize()`/`md5hex()` 是一对。踩过：一边 MD5 一边 SHA-256，
客户端去错误分片找 token，**永远零命中且不报错**。改任一边必须同时改另一边并实测。

索引现为 **512 分片**，postings 按卷宗归并成 `{doc:[pages]}`（体积减半，单片 355KB）。

**人工复核工作台**用法见 [复核工作台使用说明.md](复核工作台使用说明.md)。
判「放弃」的页导出后合并进 `unusable_pages.json`，流水线与站点两头都认这一份名单
（流水线要**重启**才读得到）。

**旋转页（已确认存在，检测未解决）**：`smpa-files-3150` p6 是确认旋转 90° 的中文页。
**三种几何判据全部失败**——长宽比、行列投影方差比 <0.55、反过来取 >2，
最后一个的确认旋转页得 2.58，而竖排正立标语页 2.65，**更高**。
**结论：几何启发式在此问题上走不通，别再试第四个判据。**
可行方案：中文占多数的页很少，对这批页在提示词里加一句方向说明，或原图与旋转图各转一次取优。
已用后者救回一页被两版 OCR 都编造的中共传单。

---

## 7. 踩过的坑（重复踩会浪费很多时间）

### 取图
1. **取图用 curl，别用 urllib** —— IA 上 urllib 会 `IncompleteRead`。
2. **别给 curl 加 `--retry-all-errors`** —— 它会让 404 也重试，吞吐从 3.5 件/秒掉到 0.22。
3. **`archive.org/metadata` 是服务端限速**，加并发无效（P=8 与 P=20 一样）；
   **文件下载则并发有效**。两条路径的结论不能混用。
4. **下载失败要删掉空壳文件** —— 否则续传会把 0 字节残留当成已完成。

### 续传判据（同一个错误换了三个地方复发，特别注意）
5. **按「成功」而非「有记录」跳过** —— 否则网络抖动造成的失败会被永久跳过，数据静默缺失。
6. **按「有没有产出」而非「有没有报错」** —— 漏掉第三种：**有记录、无 error、
   但正文是空字符串**。模型返回 HTTP 200、body 正常、content 是 `""`，全程零报错。
   实测 7,627 页里正好 10 页落在这个缝里，**永远不会被重试**。
7. **「有错误记录」不等于处理完了** —— 第一版 `verified()` 把取图失败也当成完成，
   那一件随即通过验证、**影像被删**、跑批向前，这一页就**永久空白**。
   实测 1,651 页丢了 10 页（0.6%），照这比例剩下 85,000 页会丢约 500 页，全程不报错。
   现在 `recoverable()` 区分「可修复」（取图失败/超时/抖动）与「没救」（两侧审查都拒）。

### 超时与退化
8. **`urlopen(timeout=)` 是 socket 超时，不是墙钟上限** —— 服务端只要持续吐字节就永不触发。
   实测翻译卡在一次请求上 **2.5 小时**，连接开着、CPU 只用 3.44 秒，整条流水线静默停摆。
   必须另用 `ThreadPoolExecutor.result(timeout=)` 加整体墙钟上限（`demo_run.call(deadline=)`）。
   **翻译超时的真凶是这个 90 秒 socket 读超时，不是块太大**——别再去拆小分块。
9. **翻译/转录要防退化循环** —— 模型可能吐出成千上万次重复字符，撞 max_tokens 截断，
   导致**页码标记丢失、后续整页内容消失**，而且**不报任何错**，
   只表现为「某页特别长、某页没有」。实见一页中共传单吐了 1,349 次「□」，
   把 2,327 字源文变成 14,726 字。
   `demo_run.degenerate()` 三道判据：①同字符连续重复（剥离分隔线后 `([^\s])\1{25,}`）
   ②片段重复度显著高于源文 ③产出超源文 3.5 倍。
   命中就拆半重试，仍退化则留空，**绝不写入垃圾**。
10. **退化检测误杀正确译文，踩了三次——这条判据的危险方向是「冤枉」不是「漏过」** ——
    误杀的后果比漏过严重：正确译文被整页丢掉，还被永久标成「译不出来」，
    等于**往站点上写假信息**。三次分别是：
    - SMP 报告表满纸 `File No............`，点线被判成字符重复循环；
    - 修了点线**没修等号线**，`SHANGHAI MUNICIPAL POLICE` 表头带 32 个连续 `=`，
      照样误杀，害 **14 页表格页的正确译文**被丢（实测 1370/p34 译文 638 字完全正确）；
    - CRIME DIARY 这类**表单页本身就重复**（「姓名：____ 住址：____」成排空栏），
      忠实译出来自然重复，**4 页正确译文**被丢。
    现在的做法：先 `re.sub` 一次性剥齐所有分隔线字符（`. · … - _ — – = * ~ # +` 全角空格），
    且片段重复改成**与源文比较**（只有译文比源文明显更重复才算退化），而不是看绝对值。

### 数据安全
11. **重跑某页会连带抹掉该页译文** —— 原来是整条替换 `page_data` 记录，`zh` 字段跟着没了，
    **同样不报错**。已改成合并。补跑转录前务必确认这一点。
12. **改了公共配置会静默打断正在跑的后台任务** —— 把 `common.py` 改成必须读环境变量后，
    正在跑的轮询循环读不到 key，空跑一小时才发现。现在会读同目录 `.env`。
13. **备用端点的地址字段可能存的是密钥** —— llm-ocr 插件的 `customBaseUrls.Custom`
    存的其实是 `sk-…`（插件自己配置写串了），只有 `Custom2` 是真 URL。
    取地址时必须校验 `startswith("http")`，否则会拿密钥当地址发出去。
14. **删影像前必须先算影像指标** —— `imgqual` 要读原图，删了就再也算不了。顺序不能颠倒。

### 接口
15. **百炼内容审查返回 HTTP 200，错误藏在 body 的 `error` 字段里** —— 脚本必须按这个判失败，
    不能只看状态码。
16. **中文没有空格，CSS 必须 `overflow-wrap:anywhere`** —— 中共传单那页一行 782 字，
    `white-space:pre-wrap` 不会折行，直接撑破三栏布局。grid 子项还要加 `min-width:0`。

### 监控自己也会骗人（2026-08-19 新增）
17. **监控的正则必须照着日志里真实出现过的行写，别照记忆写** ——
    08-19 断网整场事故（预取图 0/457、347 页全失败、391 次 URLError），
    `monitor.py` 报的是**「无异常信号」**。它只认 `取图 N/M`，而日志里写的是
    `↓取图结束 0/457`；URLError 和「翻译全失败」根本没在查。
    正确做法：`sed -E 's/[0-9]+/N/g' /tmp/smp-run.log | sort | uniq -c` 先把
    真实行式样列出来，再照着写正则。
18. **误报比漏报更危险，如果那条警告叫人动手** ——
    `procs()` 把只读的 `run_chunked.py --stat` 也算成流水线，于是报
    「⛔ 有 2 个 run_chunked 同时在跑，需杀掉多余的」。照做会杀掉真在跑的批。
    凡是「建议用户执行破坏性操作」的判据，必须专门测一遍会不会冤枉。

---

## 8. 给 AI 智能体的额外提醒

- **这批史料不可替换**。`~/projects/smp-archive/`、`data/*.json` 和用户的 Obsidian 库
  属于「丢了就没了」的数据，任何情况下不要原地修改或删除，要动先复制。
  代码可以随便改，有 git。
- **不要为省钱降图片分辨率**。这是本项目最重要的一条，理由见 README。
- **验证再汇报**。这个项目里已经出现过多次「参数传了但没生效」「测试写错导致假失败」
  「监控说没事其实出事」，凡是说「搞定了」之前先跑一遍看输出。
- **校准判据要测两个方向**：能不能抓到真的 + 会不会冤枉假的。
  旋转检测连错三次、监控盲区、监控误报，三次都是只测了前一半。
- **别用局部证据下全局结论**。「开思考没用」是只测了实体名得出的；换成开放性提问结论相反
  （思考模式给出 affray / ratepayers / 过激党 等行话，快速模式想不到）。
- **不要用自己写进提示词里的例子测提示词**——那等于测一张替换表。
- **文档会过时，`monitor.py` 不会**。接手时先跑监控看真实数字，别信任何文档里的进度。
- 用户是历史学研究者、不是程序员。解释用人话，给推荐项而不是罗列选项。
