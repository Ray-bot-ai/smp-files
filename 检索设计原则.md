# 检索系统设计原则

> 记录人：用户口述整理。本文档收集的是**反复出现的设计错误模式**，作为本站及后续一切检索相关系统的设计原则。每一条都对应一次真实犯过的错。

---

## 原则一：检索必须默认同时覆盖标题与正文

一个文档既然有标题也有正文，检索就**默认**应该既覆盖标题、也覆盖正文。

把检索做成「只覆盖正文、不覆盖标题」是荒谬的。标题是定位文献的第一入口，尤其在正文尚未全部转录的档案库里，标题检索是用户找到未转录卷宗的唯一途径。

**已修复**：检索页在正文命中之外并列显示「标题命中」区，覆盖全库 3,866 件标题（含未转录卷宗，链 Internet Archive 原件）。

---

## 原则二：基础检索语法是默认功能，不需要"特殊设置"

AND、OR、NOT 是最基础的检索语法，**不应要求用户开启任何设置**，天然就该可用。

同理，这些都是默认配置，不是可选项：

- **英文大小写不敏感** —— 默认；
- **中文字形转换** —— 默认，见原则三。

**反例**：任何"高级搜索"开关、任何要求用户学习特殊语法才有的能力。

---

## 原则三：中文检索必须默认内置字形转换（历史文献尤甚）

对历史文献的中文检索，字形转换是**默认配置**，包括：

1. **简体字 ↔ 繁体字**（最常见）；
2. **异体字**（次之）；
3. **大陆新字形 ↔ 港台旧字形**（较少见但同样存在，如 吴/吳、吕/呂）。

当前版本已内置简繁、异体、新旧字形转换（基于自建「简繁异体通搜」变体表 + 补充新旧字形组）。

**此后设置的其他任何检索系统，都必须带有这些功能**，而不是当作附加品。

---

## 原则四：中文单字必须可检索

让中文**单字**不可检索是荒谬的设计。

单字完全可以是合理且高频的检索词：烟草的「烟」、火炮的「炮」、工厂的「厂」……单字不可搜等于砍掉了检索系统最基础的粒度。

**已修复**：bigram 索引曾使单字只在"孤立成词"时才被索引（「工」在 12,424 页中出现，却只能命中 8 页）。现在每个 CJK 字符本身也是索引 token，单字不受停用词阈值裁切。

---

## 原则五：所有内置限制必须明文说明——静默限制等同于静默失败

系统默认内置的**任何影响检索结果的限制**，都必须让维护者和用户都知道，绝不能藏在后台。

限制分两类：

### 第一类：设置本身合理，但必须公示

例：**停用词**。停用词设计可以是合理的，但规则必须写清楚——出现在检索页面上，而不是代码里。当前站点已明文（中英双语）公示 40% 停用词规则。

### 第二类：设置本身荒谬，不应存在

例：

- 结果最多显示 40 件；
- 标题最多显示 30 条；
- AI 检索对工具调用轮数的上限；
- AI 检索最多取回件数的限制；
- AI 检索阅读正文件数的限制。

一个带有如此限制的历史学检索**实际不可用**。这些限制不应存在。

**判断标准**：对用户来说，拿到结果比等待更重要。任何"为了性能而隐藏结果"的设计都是错误方向——性能问题用工程手段解决（异步渲染、渐进加载），而不是减少用户能看到的东西。

---

## 原则六：命中展示不设上限，全部命中都要呈现

检索结果应该显示**所有命中**。不存在"前 N 条"，不存在折叠隐藏。

**已修复**：正文命中列表曾限 40 件、标题区曾限 30 条，均已删除。现在全部命中直接列出（实测单字查询 12,508 页链接、1,538 件全渲染可用）。

---

## 原则七：预览与全部必须区分，且剩余内容要以醒目形式呈现

如果用户点击某个条目看到的是**预览**而非全部内容（实际还有更多命中），那么：

1. 这一点必须让用户知情；
2. 剩余内容的存在必须以**醒目**的形式呈现——而不是用小字体藏在页面中间的某个角落，那样用户完全可能看不到。

用户看不到的提示 = 不存在。醒目意味着：有视觉区分的提示条、明确的数字（"其余 N 页命中"）、可直接点击的链接。

---

## 附：本站当前对这些原则的落实状态

| 原则 | 状态 |
|---|---|
| 一、标题+正文双覆盖 | ✅ 已实现（全库标题检索） |
| 二、AND/OR/NOT 默认 | ✅ 已实现 |
| 三、字形转换默认 | ✅ 已实现（简繁+异体+新旧字形） |
| 四、单字可搜 | ✅ 已实现（2026-08-15 修复） |
| 五、限制明文公示 | ✅ 检索页已公示全部内部设置（中英双语） |
| 六、命中不设上限 | ✅ 已实现（2026-08-15 修复） |
| 七、预览醒目提示 | ✅ 已实现（morepg 醒目黄条） |
