# 人工复核工作台 · 本地用法

## 打开

```bash
# 服务器没在跑的话先起（跑着就跳过这步）
cd ~/projects/smp-vlm-ocr
nohup .venv/bin/python -m http.server 8731 --directory docs > /tmp/httpd.log 2>&1 &
```

浏览器打开 **<http://localhost:8731/workbench.html>**

## 判页

左边原件影像，右边本站转录与译文。对着看，判这一页的转录能不能用。

| 键 | 作用 |
|---|---|
| **J** | 合格 |
| **F** | 放弃（判为不可用） |
| **← / →** | 上一页 / 下一页 |

进度存在浏览器 localStorage 里，随时关掉再回来，已判过的会自动排到队尾。

## 判完之后（关键一步）

点「导出 JSON」，把内容合并进 `unusable_pages.json`：

```bash
cd ~/projects/smp-vlm-ocr
# 把导出的内容存成 /tmp/drop.json，然后合并（自动去重）
.venv/bin/python - <<'PY'
import json
old = json.load(open('unusable_pages.json', encoding='utf-8'))
new = json.load(open('/tmp/drop.json', encoding='utf-8'))
seen = {(r['ia_id'], r['n']) for r in old}
add = [r for r in new if (r['ia_id'], r['n']) not in seen]
json.dump(old + add, open('unusable_pages.json','w',encoding='utf-8'),
          ensure_ascii=False, indent=2)
print(f'新增 {len(add)} 页，合计 {len(old)+len(add)} 页')
PY
```

合并后有两个效果，**都不需要推送**：

1. **流水线立刻跳过这些页**（`demo_run.abandoned()` 读同一份文件），
   不再为它们反复重试——这是省时间的关键。
   注意：正在跑的进程读不到，**下次重启才生效**。
2. 重跑 `build_site.py` 后，站点上这些页会标注「不可用」，
   但**内容照旧保留**（只标记不删除）。

## 刷新待复核队列

跑批会产生新的问题页。想刷新队列：

```bash
.venv/bin/python make_review.py
```

它按痛感排序挑页，不是全库都要看：
退化留空 → 缺译文 → 已判不可用（复核误判）→ 影像离群。

## 说明

- 影像从 Internet Archive 现取，**需要联网**；本地不存图。
- 本地（localhost）和线上（github.io）的进度是**分开存**的，
  因为 localStorage 按域名隔离。建议固定在一边判。
